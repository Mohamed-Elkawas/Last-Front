/** Playground IDs shown on the marketing home section (demo only). */
export const DEMO_HOME_PLAYGROUND_IDS = ["1", "2", "3"] as const

/** Tournament IDs shown on the marketing home section (demo only). */
export const DEMO_HOME_TOURNAMENT_IDS = ["1", "2"] as const

/**
 * Legacy demo IDs removed: favorites are persisted in `favorites-store` and resolved via
 * `favorites.repository` + `favorites.service`. Kept as a placeholder file for docs only.
 */

export {}
